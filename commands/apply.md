---
description: Apply approved plan via aria-coder + quality gate
---

## Steps
1. `codex-save.sh "implement per @var:codex_plan"`
2. Task tool → aria-coder: Apply from codex_last, run quality-gate.sh
3. PASS → Done | FAIL → Fix or `/thinking`

## Prerequisites
Run `/plan <task>` first.
